# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
/build/
/windows/
/backend/moca_densenet.pth
/graphify-out
/test_hf.py
/test_sentence.wav
/typhoon-hf-model/
/test_ct2.py
/typhoon-large-v3-ct2/
# Downloaded model checkpoints. Multi-gigabyte binaries that `git add -A` will
# otherwise sweep into a commit — systran-whisper/model.bin alone is 1.5 GB.
# Everything the backend actually loads lives under backend/models/, which is
# ignored by backend/.gitignore and rebuilt by scripts/convert_model.py.
/systran-whisper/
# ...and the archives of them. The model *directories* above were ignored but
# `systran-whisper.zip` (1.4 GB) and `backend/models.zip` (4.1 GB) were not —
# both appear when the checkpoints are packed up to hand to someone rather than
# re-downloaded, which is exactly when a `git add -A` is likeliest. A 99 MB
# blob is already unremovable from this history without a force-push; these are
# forty times larger.
*.zip
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

/build/app/
/build/path_provider_android
/build/permission_handler_android
# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/



# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
